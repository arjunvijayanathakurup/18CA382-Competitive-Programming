#include<bits/stdc++.h>

using namespace std;

void addVector(int n, int p, vector<int> adj[]){
    
}

void printVector(vector<int> adj[]){
    while(p){
        for(int i = 0; i < n; i++){
            cout<<adj[i]
        }
    }
}

int main(){
    vector<int> adj[n];
    int n, p, buf;
    cin>>n;
    cin>>p;
    adj[n].push_back(p);
    printVector(adj);
    return 0;
}